main:
  module: evaluate
  option:
    - --max-evaluators 3
    - --device cuda cpu

dataset:
  - module: HCR.FvT.Eval
    option:
      - --metadata coffea4bees/metadata/datasets/
      - --max-workers 8
      - --data-source detector
      - --friends "" coffea4bees/metadata/datasets/classifier_inputs_ttHbb_v3.json@@HCR_input

model:
  - module: HCR.FvT.baseline.Eval
    option:
      - --models
      - Final
      - "{model}/result.json"

analysis:
  - module: kfold.Merge
    option:
      - --name FvT
      - --step 100000
      - --workers 5
      - --clean

setting:
  - module: IO
    option:
      - output: "{FvT}"
  - module: ml.DataLoader
    option:
      - num_workers: 8
